import type { Transaction } from './types.js';

const escapeHtml = (value: unknown) => String(value ?? '').replace(/[&<>"']/g, character => ({
  '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
})[character]!);

const badge = (value: string) =>
  `<span class="badge badge-${escapeHtml(value.toLowerCase())}">${escapeHtml(value.replaceAll('_', ' '))}</span>`;

const styles = `
:root{color-scheme:dark;--bg:#0a0c0f;--panel:#11151a;--panel2:#151a20;--line:#293039;--muted:#929ba7;--text:#edf0f4;--green:#70dfa9;--amber:#f7c66d;--red:#ff8c95;--blue:#84b8ff}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px/1.5 Inter,ui-sans-serif,system-ui,-apple-system,sans-serif}a{color:inherit;text-decoration:none}button,input,textarea{font:inherit}header{height:64px;border-bottom:1px solid var(--line);display:flex;align-items:center;justify-content:space-between;padding:0 max(24px,calc((100% - 1180px)/2));position:sticky;top:0;background:rgba(10,12,15,.96);z-index:2}.brand{font-weight:800;letter-spacing:.04em}.brand-mark{color:var(--green);margin-right:10px}.header-meta{color:var(--muted);font-size:12px}.header-meta b{color:var(--green);margin-right:6px}main{max-width:1180px;margin:auto;padding:36px 24px 80px}h1{font-size:28px;line-height:1.2;margin:0 0 8px;letter-spacing:-.025em}h2{font-size:12px;text-transform:uppercase;letter-spacing:.12em;color:var(--muted);margin:0 0 18px}h3{font-size:16px;margin:0 0 10px}.muted{color:var(--muted)}.eyebrow{font:700 11px ui-monospace,SFMono-Regular,monospace;letter-spacing:.08em;color:var(--muted);text-transform:uppercase}.summary{display:flex;justify-content:space-between;gap:20px;align-items:end;margin-bottom:28px}.metrics{display:grid;grid-template-columns:repeat(5,1fr);gap:12px}.card{background:var(--panel);border:1px solid var(--line);border-radius:9px;padding:20px}.metric{min-height:104px}.metric strong{display:block;font-size:29px;margin-top:12px;font-variant-numeric:tabular-nums}.table{margin-top:22px;padding:0;overflow:hidden}.row{display:grid;grid-template-columns:minmax(280px,2fr) .75fr .65fr .8fr;gap:14px;padding:16px 20px;border-top:1px solid var(--line);align-items:center}.row:first-child{border:0}.row-link:hover{background:#161b21}.row-title{font-weight:700}.row-subtitle{display:block;margin-top:3px;color:var(--muted);font-size:12px}.empty{text-align:center;padding:58px;color:var(--muted)}.badge{display:inline-flex;align-items:center;border-radius:4px;padding:4px 8px;background:#2d343d;color:#d9dee5;font-size:10px;font-weight:800;letter-spacing:.04em;text-transform:uppercase;white-space:nowrap}.badge-high,.badge-critical,.badge-blocked,.badge-failed,.badge-rejected,.badge-rollback_failed{background:#3d2025;color:var(--red)}.badge-medium,.badge-awaiting_approval,.badge-rolling_back{background:#3a3020;color:var(--amber)}.badge-low,.badge-committed,.badge-verified,.badge-rolled_back,.badge-approved{background:#17352a;color:var(--green)}.badge-production{background:#382128;color:#ffadb3}.badge-development{background:#1b3047;color:#a7ccff}.detail-header{margin:24px 0 28px}.detail-header-line{display:flex;justify-content:space-between;gap:20px;align-items:start}.metadata{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:16px}.detail-grid{display:grid;grid-template-columns:minmax(0,1.75fr) minmax(290px,.85fr);gap:18px}.stack{display:grid;gap:18px}.resource-heading{display:flex;justify-content:space-between;gap:12px}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.changes{margin-top:16px}.change{display:grid;grid-template-columns:minmax(145px,.75fr) 1.4fr;gap:8px 20px;padding:15px 0;border-top:1px solid var(--line)}.change-values{font:600 14px ui-monospace,SFMono-Regular,monospace;overflow-wrap:anywhere}.before{text-decoration:line-through;color:#a6adb6}.arrow{color:var(--green);padding:0 8px}.meaning{grid-column:2;color:#bdc4cc}.impact{border-left:3px solid var(--green);padding:12px 16px;background:#12251e;margin-top:14px;color:#c9f5df}.blast-list{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.dependency{border:1px solid var(--line);background:var(--panel2);padding:7px 10px;border-radius:5px;font-family:ui-monospace,SFMono-Regular,monospace;font-size:12px}.risk-score{display:flex;align-items:baseline;gap:10px;margin-bottom:14px}.risk-score strong{font-size:34px}.reason{display:flex;justify-content:space-between;gap:12px;padding:11px 0;border-top:1px solid var(--line)}.reason-points{font-weight:800;font-variant-numeric:tabular-nums}.positive{color:var(--red)}.negative{color:var(--green)}.rollback-ok{color:var(--green)}.rollback-no{color:var(--red)}.actions{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-top:18px}.actions .full{grid-column:1/-1}button{border:1px solid transparent;border-radius:5px;padding:10px 15px;font-weight:800;cursor:pointer}.approve{background:var(--green);color:#07130d}.reject{background:#311f24;border-color:#5b2b33;color:#ffabb3}.execute{background:var(--blue);color:#071426}.rollback{background:#332e20;border-color:#5a4a25;color:#ffda89}button:disabled{opacity:.55;cursor:wait}.timeline{border-left:2px solid #343c46;padding-left:20px}.event{position:relative;margin:0 0 21px}.event:before{content:'';position:absolute;width:8px;height:8px;border:2px solid #687482;background:var(--panel);border-radius:50%;left:-26px;top:5px}.event time{display:block;color:var(--muted);font-size:11px;margin-top:3px}.result{padding:12px;background:var(--panel2);border:1px solid var(--line);border-radius:5px}dialog{width:min(440px,calc(100% - 32px));border:1px solid #3b444f;border-radius:9px;background:var(--panel);color:var(--text);padding:24px;box-shadow:0 24px 80px #000}dialog::backdrop{background:#050608cc}label{display:block;font-weight:700;margin:15px 0 6px}input,textarea{width:100%;background:#0b0e12;color:var(--text);border:1px solid #3a424d;border-radius:5px;padding:10px}textarea{resize:vertical;min-height:76px}.dialog-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:20px}.error{color:var(--red);min-height:21px}.back:hover{color:var(--text)}@media(max-width:800px){header{position:static}.summary{display:block}.metrics{grid-template-columns:repeat(2,1fr)}.detail-grid{grid-template-columns:1fr}.row{grid-template-columns:1fr auto}.row>span:nth-child(2){display:none}.table-header{display:none}.detail-header-line{display:block}.change{grid-template-columns:1fr}.meaning{grid-column:1}.actions{grid-template-columns:1fr}.actions .full{grid-column:auto}}`;

function layout(title: string, body: string, script = '') {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="Change intelligence and transactional safety for autonomous agents"><title>${escapeHtml(title)} · Agent Transaction</title><style>${styles}</style></head><body><header><a class="brand" href="/"><span class="brand-mark">◈</span>AGENT TRANSACTION</a><span class="header-meta"><b>●</b> Gateway online</span></header><main>${body}</main>${script ? `<script>${script}</script>` : ''}</body></html>`;
}

export function dashboard(transactions: Transaction[]) {
  const count = (predicate: (transaction: Transaction) => boolean) => transactions.filter(predicate).length;
  const rows = transactions.map(transaction => `<a class="row row-link" href="/transactions/${encodeURIComponent(transaction.id)}">
    <span><span class="row-title">${escapeHtml(transaction.intent)}</span><span class="row-subtitle">${escapeHtml(transaction.agent)} · ${escapeHtml(transaction.operation)} · <span class="mono">${escapeHtml(transaction.resource)}</span></span></span>
    <span>${badge(transaction.environment)}</span><span>${badge(transaction.risk.level)} <span class="muted">${transaction.risk.score}</span></span><span>${badge(transaction.status)}</span>
  </a>`).join('');
  return layout('Transactions', `<div class="summary"><div><p class="eyebrow">Control plane</p><h1>Agent transactions</h1><p class="muted">Understand consequential changes before they reach infrastructure.</p></div><div class="muted">${transactions.length} transaction${transactions.length === 1 ? '' : 's'} in this workspace</div></div>
    <section class="metrics">${[
      ['Pending approvals', count(t => t.status === 'AWAITING_APPROVAL')],
      ['High risk', count(t => ['HIGH', 'CRITICAL'].includes(t.risk.level))],
      ['Committed', count(t => t.status === 'COMMITTED')],
      ['Blocked', count(t => t.status === 'BLOCKED')],
      ['Rolled back', count(t => t.status === 'ROLLED_BACK')],
    ].map(([label, value]) => `<div class="card metric"><span class="muted">${label}</span><strong>${value}</strong></div>`).join('')}</section>
    <section class="card table"><div class="row table-header eyebrow"><span>Change / agent</span><span>Environment</span><span>Risk</span><span>State</span></div>${rows || '<div class="empty">No transactions yet. Connect an MCP agent or run <span class="mono">npm run db:seed</span>.</div>'}</section>`);
}

export function detail(transaction: Transaction) {
  const reversible = transaction.reversibility === 'FULLY_REVERSIBLE';
  const controls = transaction.status === 'AWAITING_APPROVAL'
    ? `<button class="approve" data-action="approve">Approve</button><button class="reject" data-action="reject">Reject</button>`
    : transaction.status === 'APPROVED'
      ? `<button class="execute full" data-action="execute">Execute approved change</button>`
      : transaction.status === 'COMMITTED' && reversible
        ? `<button class="rollback full" data-action="rollback">Rollback transaction</button>` : '';
  const verification = transaction.verification ? `<div class="result"><b>${badge(transaction.verification)}</b><p class="muted">Expected state was compared with the actual provider state after execution.</p></div>` : '<p class="muted">Verification begins after provider execution.</p>';
  const body = `<a class="muted back" href="/">← All transactions</a><section class="detail-header"><p class="eyebrow">AI change plan · ${escapeHtml(transaction.id)}</p><div class="detail-header-line"><div><h1>${escapeHtml(transaction.intent)}</h1><p class="muted">Proposed by <b>${escapeHtml(transaction.agent)}</b> in session <span class="mono">${escapeHtml(transaction.sessionId)}</span></p></div><div class="metadata">${badge(transaction.environment)} ${badge(transaction.status)}</div></div></section>
  <div class="detail-grid"><div class="stack"><section class="card"><h2>Proposed change</h2><div class="resource-heading"><h3 class="mono">${escapeHtml(transaction.resource)}</h3><span class="muted">${escapeHtml(transaction.operation)}</span></div><div class="changes">${transaction.changes.map(change => `<div class="change"><b>${escapeHtml(change.field)}</b><span class="change-values"><span class="before">${escapeHtml(JSON.stringify(change.before))}</span><span class="arrow">→</span>${escapeHtml(JSON.stringify(change.after))}</span><span class="meaning">${escapeHtml(change.meaning)}</span></div>`).join('') || '<p class="muted">No normalized field changes detected.</p>'}</div>${transaction.changes[0] ? `<div class="impact"><b>Semantic impact</b><br>${escapeHtml(transaction.changes.map(change => change.meaning).join(' '))}</div>` : ''}</section>
  <section class="card"><h2>Blast radius</h2><h3>${transaction.customerFacing ? 'Customer-facing workload' : 'Internal workload'} · ${transaction.dependencies.length} linked resource${transaction.dependencies.length === 1 ? '' : 's'}</h3><p class="muted">${transaction.customerFacing ? 'Degradation could be visible to customers.' : 'No customer-facing dependency was declared.'}</p><div class="blast-list">${transaction.dependencies.map(dependency => `<span class="dependency">${escapeHtml(dependency)}</span>`).join('') || '<span class="muted">No provider dependencies discovered.</span>'}</div></section>
  <section class="card"><h2>Execution & verification</h2>${transaction.execution ? `<p><b>Provider request</b> <span class="mono">${escapeHtml(transaction.execution.providerRequestId || 'pending')}</span></p>` : '<p class="muted">No provider mutation has been sent.</p>'}${verification}</section>
  <section class="card"><h2>Immutable audit trail</h2><div class="timeline">${transaction.audit.map(event => `<div class="event"><b>${escapeHtml(event.message)}</b><time>${escapeHtml(new Date(event.at).toLocaleString('en-US', { dateStyle: 'medium', timeStyle: 'medium', timeZone: 'UTC' }))} UTC · ${escapeHtml(event.actor)}</time></div>`).join('')}</div></section></div>
  <aside class="stack"><section class="card"><h2>Explainable risk</h2><div class="risk-score">${badge(transaction.risk.level)}<strong>${transaction.risk.score}</strong><span class="muted">/ 100</span></div>${transaction.risk.reasons.map(reason => `<div class="reason"><span><b>${escapeHtml(reason.reason)}</b><br><small class="muted">${escapeHtml(reason.evidence)}</small></span><span class="reason-points ${reason.points >= 0 ? 'positive' : 'negative'}">${reason.points > 0 ? '+' : ''}${reason.points}</span></div>`).join('')}</section>
  <section class="card"><h2>Rollback guarantee</h2><h3 class="${reversible ? 'rollback-ok' : 'rollback-no'}">${reversible ? '✓ FULLY REVERSIBLE' : '⚠ ' + escapeHtml(transaction.reversibility.replaceAll('_', ' '))}</h3><p class="muted">${reversible ? 'Captured configuration can be restored through a compensating action and verified against before-state.' : 'Automated rollback is not offered because restoration cannot be guaranteed.'}</p></section>
  <section class="card"><h2>Policy decision</h2><p>${badge(transaction.policy)}</p><p class="muted">${escapeHtml(transaction.policyReason)}</p>${controls ? `<div class="actions">${controls}</div>` : ''}</section></aside></div>
  <dialog id="action-dialog"><form method="dialog"><h3 id="dialog-title">Review transaction</h3><p class="muted" id="dialog-description"></p><label for="token">Operator approval token</label><input id="token" name="token" type="password" autocomplete="current-password" required><label for="comment">Audit comment</label><textarea id="comment" name="comment" maxlength="500" placeholder="Why is this decision appropriate?"></textarea><p class="error" id="action-error" role="alert"></p><div class="dialog-actions"><button value="cancel">Cancel</button><button class="approve" id="confirm" value="default">Confirm</button></div></form></dialog>`;
  const script = `const dialog=document.querySelector('#action-dialog');let action='';document.querySelectorAll('[data-action]').forEach(button=>button.addEventListener('click',()=>{action=button.dataset.action;document.querySelector('#dialog-title').textContent=action[0].toUpperCase()+action.slice(1)+' transaction';document.querySelector('#dialog-description').textContent=action==='rollback'?'Restore the captured before-state and verify the result.':'This decision is recorded in the audit trail.';dialog.showModal()}));dialog.addEventListener('close',async()=>{if(dialog.returnValue!=='default')return;const button=document.querySelector('#confirm');const error=document.querySelector('#action-error');button.disabled=true;error.textContent='';try{const response=await fetch('/api/transactions/${encodeURIComponent(transaction.id)}/'+action,{method:'POST',headers:{'content-type':'application/json','x-approval-token':document.querySelector('#token').value},body:JSON.stringify({approver:'web-operator',comment:document.querySelector('#comment').value||'Reviewed in Agent Transaction'})});const payload=await response.json();if(!response.ok)throw new Error(payload.error||'Request failed');location.reload()}catch(reason){error.textContent=reason.message;button.disabled=false;dialog.showModal()}});`;
  return layout(`Change plan ${transaction.id}`, body, script);
}
